apiVersion: v1
kind: Service
metadata:
  name: media-processor-service
  namespace: 4chan-v2
  labels:
    app: media-processor
spec:
  selector:
    app: media-processor
  ports:
  - name: http
    port: 8081
    targetPort: 8081
    protocol: TCP
  type: ClusterIP
